#include "holberton.h"

/**
 *print_last_digit - Prints the last digit
 *of a number
 *
 *@y: input number is an integer.
 *
 *Return: last digit.
 */
int print_last_digit(int y)
{
int l;

l = y % 10;
if (1 < 0)
{
_putchar(-l + 48);
return (-l);
}
else
{
_putchar(l + 48);
return (l);
}
}
